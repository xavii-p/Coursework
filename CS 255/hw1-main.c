#include<stdio.h>
#include<stdlib.h>

void rem(const char *s);
void perfect(long num);
void nearest(void);

int main(int argc, char const *argv[])
{
    
    // rem(argv[1]);
    /* for (int i = 0; i < 30; i++)
    {
        perfect(i);
    } */

    nearest();
    return 0;

}